import styled from "styled-components";
import { White } from "../colors";
import { Light, MalgunGothic } from "../Fonts";

export const TextFooter = styled.p`
    ${MalgunGothic}
    ${Light}
    font-size: 13px;
    color: ${White}
`